#include "temp/math.hpp"

// For this simple example, everything is inline in the header.
// If you prefer, you can move the implementations here
// (but leaving them inline is fine for testing purposes).
